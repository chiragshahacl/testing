export enum WORKER_FLOWS {
  CONNECT = 'connect',
  DISCONNECT = 'disconnect',
  TEST_DISCONNECTION = 'testDisconnection',
  UPDATE_SELECTED_PATIENT = 'updateSelectedPatient',
  UPDATE_SELECTED_GROUP = 'updateSelectedGroup',
}
